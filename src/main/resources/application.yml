server:
    port: 8091

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: admin715
    url: jdbc:postgresql://simulador.cslrhqcrlea5.us-east-2.rds.amazonaws.com:5432/postgres
    platform: postgres
    initialization-mode: always
    continue-on-error: true
  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:
#     ddl-auto: create
    database: postgresql
  logging:
    pattern:
      console: "%d{yyyy-MM-dd HH:mm:ss} ${LOG_LEVEL_PATTERN:-%5p} %m%n"
